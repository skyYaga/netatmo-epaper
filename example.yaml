# Optional: enable development mode
devMode: False

netatmo:
  # Name of the Netatmo home
  homeName:
  # Name of the Netatmo indoor module
  indoorName:
  # Name of the Netatmo outdoor module
  outdoorName:
  # Name of the Netatmo rain gauge module
  rainGaugeName: 
  oauth:
    # DEPRECATED - Netatmo OAuth client id
    # clientId: 
    # DEPRECATED - Netatmo OAuth client secret
    # clientSecret:
    # DEPRECATED - Netatmo OAuth refresh token
    # refreshToken:


owm:
  # API Token for openweathermap
  apiToken:
  # openweathermap latitude
  lat:
  # openweathermap longitude
  lon: